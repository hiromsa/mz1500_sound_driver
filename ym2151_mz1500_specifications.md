# MZ-1500 YM2151(OPM) 拡張ボード プログラミング仕様書

将来のMMLエディタ・プレーヤー開発に向けて、MZ-1500上でのYM2151のハードウェアI/O制御および基本機能に関する仕様をまとめました。

---

## 1. I/Oポート仕様（MZ-1500拡張ボード環境）

MZ-1500においてYM2151にアクセスするために割り当てられたI/Oポートは以下の通りです。

| I/Oアドレス | R/W | 用途 |
| :--- | :--- | :--- |
| `0708h` | Write | **レジスタ・アドレスポート**<br>YM2151の内部レジスタ番号（00h〜FFh）を指定します。 |
| `0708h` | Read  | **未定義 / 存在確認用**<br>本エミュレータではハードウェア接続確認用として常に `00h` を返します。実機では不定値か `00h` の可能性があります。 |
| `0709h` | Write | **データポート**<br>`0708h` で指定したレジスタに対してデータを書き込みます。 |
| `0709h` | Read  | **ステータス・レジスタ**<br>YM2151の現在の状態を読み取ります。主にBUSYフラグやタイマーの確認に使用します。 |

> [!CAUTION]
> MZ-1500の標準I/Oは8bit（00h〜FFh）ですが、本ボードは **16bit（0708h/0709h）** のI/Oアドレスを使用します。アセンブラで記述する場合は `OUT (C), A`（Z80命令）を使用して `BC` レジスタにアドレスを設定する必要があります。<br>
> ※BASICの `OUT` 命令では処理系によって16bitアクセスがサポートされない、または下位8bit（`08h`, `09h`）としてデコードされる仕様になっている場合があるため注意してください。

---

## 2. データの書き込み手順（BUSYウェイト）

YM2151は処理速度に限界があるため、データを連続して書き込むと内部で取りこぼしが発生します。書き込みを行う前には必ず `0709h` のステータス・レジスタを読み込み、**BUSYフラグ（Bit 7）** が `0` になるのを待つ必要があります。

**アセンブリでの記述例:**
```z80
; YM2151へデータを書き込むサブルーチン
; [入力] A: レジスタ番号, E: 書き込むデータ
WRITE_YM2151:
    LD BC, 0709h
WAIT_BUSY:
    IN D, (C)        ; ステータスレジスタ(0709h)を読み込み
    BIT 7, D         ; BUSYフラグ(Bit7)をチェック
    JR NZ, WAIT_BUSY ; 1ならBUSYなのでループして待機

    DEC C            ; BC = 0708h (アドレスポート)
    OUT (C), A       ; レジスタ番号を書き込み
    INC C            ; BC = 0709h (データポート)
    OUT (C), E       ; データを書き込み
    RET
```

**ステータス・レジスタ (Read: 0709h) のビットアサイン:**
- `Bit 7` (BUSY) : 1 = 処理中 (書き込み不可)、0 = レディ (書き込み可)
- `Bit 1` (Timer B) : 1 = タイマーBがオーバーフローした
- `Bit 0` (Timer A) : 1 = タイマーAがオーバーフローした

---

## 3. YM2151 (OPM) の主要レジスタマップ概要

YM2151は8チャンネルのFM音源（4オペレータ）を持ちます。チャンネル番号(`ch`)は 0〜7、オペレータ番号(`op`)は 0〜3 です。

| レジスタ (Hex) | 略称 | 機能・設定内容 |
| :--- | :--- | :--- |
| `01` | TEST | LFOリセットなど（通常は `02` (LFOリセット) を書き込んだ後 `00` に戻す） |
| `08` | KON | キーオン／キーオフ (Bit 0-2: ch指定, Bit 3-6: 鳴らすOP指定 M1,C1,M2,C2) |
| `0F` | NE / NFRQ | ノイズイネーブル (Bit 7)、ノイズ周波数 (Bit 0-4) |
| `10`〜`12` | CLKA1/2, CLKB | タイマーA / B の周期設定 |
| `14` | CSM / FLAG | タイマー制御、フラグクリア (Bit 7: IRQイネーブルAなど) |
| `18` | LFRQ | LFO周波数 |
| `19` | PMD / AMD | LFOのピッチ変調深度(PMD) / アンプ変調深度(AMD) (Bit 7で切り替え) |
| `1B` | CT / W | LFO波形 (Bit 0-1) と 外部制御端子(CT1, CT2)出力 |
| `20`〜`27` | RL / FL / CON | 左右パン(Bit 6-7)、フィードバック(Bit 3-5)、アルゴリズム(Bit 0-2) |
| `28`〜`2F` | KC | キーコード (音程・ノート番号) |
| `30`〜`37` | KF | キーフラクション (音程の微調整・ピッチベンド用) |
| `38`〜`3F` | PMS / AMS | LFO変調の感度設定 (PMS: ピッチ, AMS: アンプ) |
| `40`〜`5F` | DT1 / MUL | デチューン1 (Bit 4-6)、マルチプル (Bit 0-3) |
| `60`〜`7F` | TL | トータルレベル (音量・モジュレーション強度) `00`が最大, `7F`が最小 |
| `80`〜`9F` | KS / AR | キースケール(Bit 6-7)、アタックレート(Bit 0-4) |
| `A0`〜`BF` | AMS-EN / D1R | AMS有効化(Bit 7)、ディケイ1レート(Bit 0-4) |
| `C0`〜`DF` | DT2 / D2R | デチューン2(Bit 6-7)、ディケイ2レート(Bit 0-4) |
| `E0`〜`FF` | D1L / RR | ディケイ1レベル(Bit 4-7)、リリースレート(Bit 0-3) |

> **アドレス計算方法:**
> オペレータ固有のパラメータ（40h〜FFh）は `Base Address + (Operator Offset) + Channel` で指定します。
> - Operator Offset: OP1(`00h`), OP2(`08h`), OP3(`10h`), OP4(`18h`)
> - 例: チャンネル3 (03h) の OP2 の TL (Base 60h) ＝ `60h + 08h + 03h = 6Bh`

## 4. 初期化と発音までの基本フロー
MMLプレーヤーでYM2151を初期化し、音を鳴らすための基本手順です。

1. **サウンドチップのリセット**
   - レジスタ `01h` に `02h` を書き込む（LFOリセット）
   - レジスタ `01h` に `00h` を書き込む
   - すべてのレジスタ（`00h`〜`FFh`）に `00h`（または初期値）を書き込んで初期化。特にKON（`08h`）は全チャンネルに対して `00h` を送りキーオフしておく。
2. **音色の設定**
   - アルゴリズム(CON)とフィードバック(FL)、パン(RL)の設定（`20h`〜`27h`）
   - 各オペレータのパラメータ（MUL, TL, AR, D1R, D2R, D1L, RR 等）を順次書き込みます。
   - ※MMLプレーヤーでは、これら一連のパラメータを配列（ボイスセット）から読み込んで一括書き込みする関数を用意すると便利です。
3. **音階（キーコード）の設定**
   - 鳴らしたい音階に対応する値（KC: `28h`〜`2Fh`）と微調整（KF: `30h`〜`37h`）を書き込みます。
4. **発音（キーオン）**
   - レジスタ `08h` (KON) に対して、「鳴らしたいチャンネル(0〜7) ＋ 使用するオペレータのビット」を書き込むと発音します。
   - 例: チャンネル0で全OPをオンにする場合、`08h` に `78h` (`01111000b`) を書き込みます。
5. **消音（キーオフ）**
   - ノートオフのタイミングで、レジスタ `08h` に「チャンネル番号 ＋ OPビット全て0」を書き込みます。
   - 例: チャンネル0をオフにする場合、`08h` に `00h` を書き込みます。

## 5. 動作クロック仕様と切替対応（4.0MHz / 3.58MHz）

### 5.1 標準動作クロック
本プロジェクトにおけるYM2151(OPM)拡張ボードの標準動作クロックは **4.0 MHz (4,000,000 Hz)** とします。

- **背景と経緯**:
  - MZ-1500本体のシステムクロックは **3.58 MHz (3.579545 MHz)** ですが、当時の誌上記事（Oh!MZ 1988年1月号「MZでX1用拡張ボードを使う」等）や実機用拡張ボード（ぽよこまだんな氏製作の「ACZ-8BS1MZ」等）の多くは、ボード上に独立した **4MHz 水晶発振モジュール** を搭載してYM2151を4MHz駆動させる構成が一般的です。
  - 本サウンドドライバおよびプレビュープレイヤシステムでは、この実機拡張カードの主流構成に合わせて **4.0MHz** を基本動作クロックとして採用しています。

### 5.2 3.58MHz 切替柔軟性と設計方針
- **クロック切り替え機能**:
  - 実機の拡張FM音源カード「ACZ-8BS1MZ」等には物理的なクロック切替スイッチ（DIP SW / ジャンパ）が装備されており、システムクロック(3.58MHz)と専用発振器(4MHz)を選択できるようになっています。
  - 本プロジェクトのエミュレーション層（`YM2151Manager`）においても、デフォルトを `4,000,000 Hz` としつつ、引数や `SetClock(uint clock)` メソッドにより **3.58 MHz (3,579,545 Hz)** や任意のクロック周波数へ簡単に変更・切り替えができる柔軟な構造を採用しています。
  - 今後、MMLコンパイラや再生環境でクロック周波数の切り替え設定が必要になった場合にもスムーズに対応可能な設計となっています。

---

## 6. まとめ・現在の開発ステータス

- **ハードウェアI/O制御**: I/Oポート `0708h` (アドレス) / `0709h` (データ/ステータス) アクセスを定義。
- **動作クロック**: **4.0 MHz (4,000,000 Hz)** を標準運用。3.58 MHz への切り替えにも対応。
- **エミュレーション**: Windowsプレビュー環境での `YM2151Manager` により4.0MHz動作を忠実に再生確認可能。
