---
name: fm-editor-spec
description: FM音源GUIエディタ 実装およびUIデザイン要求仕様書
---
# FM音源GUIエディタ 実装およびUIデザイン要求仕様

## 1. 概要
MZ-1500 Sound Systemの統合環境（IDE）内で動作する、FM音源（YM2151）用のGUI音色エディタ。MMLのテキストベースでの高速な入力を阻害せず、GUIでの視覚的なパラメータ調整と双方向でデータを連動させる。初心者から熟練者まで、目的に応じて最適なアプローチで音作りを行えるモダンな開発環境を提供する。

## 2. UI/UXデザイン要件（VS Codeスタイル）
レトロPC界隈のエンジニア層が好む、ストイックで効率的な「VS Code（統合開発環境）風」のモダンデザインを採用する。以下のガイドラインに従って、Avalonia UI（XAML）による画面構築を行うこと。

*   **ベーステーマ:** プロフェッショナル向けのダークテーマ。無駄なグラデーションやドロップシャドウを完全に排した「フラット＆ミニマル」なデザインとする。
*   **カラーパレットの統一:** 
    *   背景色: 深みのあるソリッドなダークグレー（例: `#1E1E1E` または `#252526`）。
    *   パネル背景/区切り: 要素の区切りは枠線（Border）ではなく、背景色のわずかな明度差（例: `#2D2D30`）で表現する。
    *   文字色: 可読性の高い明るいグレー（例: `#CCCCCC`）。
    *   アクセントカラー: アクティブなスライダーやフォーカスされている要素には、VS Codeを彷彿とさせる単一の鮮やかな色（例: ブルー `#007ACC` または シアン）を使用し、視線を誘導する。
*   **レイアウト規則 (8ptグリッド):** コントロール間の余白（Margin / Padding）は「4, 8, 16, 24」など、4または8の倍数で厳格に統一し、情報密度が高く整然とした配置（高密度なコントロールパネル）にする。
*   **角丸 (Rounded Corners):** ボタンや入力フィールドの角はわずかな丸み（`CornerRadius="3"` 程度）にとどめ、ストイックさを損なわないシャープな印象を維持する。

## 3. 起動とデータ連携（入出力・バインディング）
*   **起動トリガー:** MMLエディタ（AvaloniaEdit等）上で、`@FM[番号] = { ... }` の定義ブロック内にテキストカーソルが存在する状態で右クリックし、コンテキストメニューから「FM音色エディタを開く」を選択することで起動（別ウィンドウまたはモーダルダイアログ）。
*   **初期データ読み込み:** 起動時に、選択中のMMLテキストをパースし、ViewModelの46個のプロパティに流し込む。
*   **MMLへの反映 (双方向バインディング):** 
    *   「反映 (Apply)」または「OK」ボタンを押下した際、現在のGUI上のパラメータ（ViewModel）をテキストフォーマット（`46パラメータのカンマ区切り`）にシリアライズする。
    *   MMLエディタ側の元の定義ブロック部分の文字列を置換・更新し、ウィンドウを閉じる。

## 4. 画面レイアウトとUIコンポーネント構成
UIはMVVMパターンに基づき、以下の3つのセクションで構成する。

### 4.1 ヘッダー部（グローバル設定）
*   **アルゴリズム (ALG):** 0〜7の選択。各アルゴリズムの接続構造（直列/並列）が視覚的にわかる簡潔なブロック図アイコンを付与したトグルボタンを8つ横並びに配置。
*   **フィードバック (FB):** 0〜7の数値指定。水平スライダーと数値入力コントロール（NumericUpDown等）をバインドして配置。

### 4.2 オペレータ編集部（OP1〜OP4）
4つのオペレータのパラメータ群を2行2列、または横4列のグリッドで整然と配置する。
*   **視覚的補助:** 選択中のALGに応じて、該当するオペレータが「キャリア（出力あり）」か「モジュレータ（変調のみ）」かを判定し、パネルの背景色（ヘッダー部分等）をわずかに変化させて役割を区別する。

各オペレータパネル内の構成要素：
*   **ヘッダー:** 「OP1 (M1)」等のラベル表示。
*   **[Mute] / [Solo] トグルボタン:** VS Codeのデバッグアイコンのような小さくフラットなボタンで配置。
*   **コピー＆ペースト:** パネル右上に、オペレータ単位のパラメータ（11項目）をコピー/ペーストするためのフラットなアイコンボタンを配置。
*   **トータルレベル (TL):** 0〜127（無音）。最も操作頻度が高いため、視認性の高い縦型スライダーで配置する。
*   **ピッチ系パラメータ:** `MUL` (0〜15)、`DT1` (0〜7)、`DT2` (0〜3)。シンプルでコンパクトなスピンボックス（数値入力）で配置。
*   **エンベロープ表示＆編集部（最重要UI）:**
    *   AR, D1R, D2R, RR (0〜31)、D1L (0〜15) の数値を入力するスピンボックスを横並びに配置。
    *   その上部に、これらの数値から擬似的に計算した**エンベロープの推移形状を折れ線グラフ（Canvas）で視覚化**して描画する。
    *   グラフの頂点（ノード）をマウスでドラッグすることで、連動する数値を増減させ、グラフ全体を動的に再描画するインタラクティブ機能を持たせる。アクセントカラーを用いて現在ドラッグ中のノードを強調する。

### 4.3 フッター部（プレビューと操作）
*   **プレビュー鍵盤:** マウスでクリック可能なフラットデザインの簡易ピアノロール（1〜2オクターブ程度）を配置。
*   **リアルタイム発音機能:** 鍵盤のクリック、またはPCキーボードの押下イベントをフックし、内部のプレビュープレイヤ（NAudioエミュレーション）へ現在のGUIパラメータを動的に送信し、リアルタイムに発音・微調整を行えるようにする。
*   **操作ボタン:** 右下に「反映 (Apply)」「キャンセル (Cancel)」のソリッドなボタンを配置。主導線となる「反映」ボタンのみアクセントカラーで塗りつぶす。

## 5. 内部ロジックおよび実装時の注意事項
*   **UIとロジックの完全分離 (MVVM):** 46個のプロパティと通知イベント（`INotifyPropertyChanged` 等）を持つViewModelを構築し、UI側のXAML実装（見た目）とデータ処理/MMLシリアライズ処理（裏側）を完全に分離すること。
*   **OPMのレジスタ順序の罠 (OP1→OP3→OP2→OP4) の吸収:** MMLテキスト上の人間向けの記述順序（OP1→OP2→OP3→OP4）と、ハードウェアの実際のレジスタ配置順序の差異によるバグを防ぐため、ViewModelとシリアライズロジック間で順序の入れ替えを完全に隠蔽・吸収すること。GUI上は人間が理解しやすい順序で表示する。
*   **波形プレビュー（オシロスコープ）の除外:** 合成波形のCanvas描画機能については、初期開発フェーズではスコープ外とする。まずはエンベロープ形状のグラフ化と高速なレスポンスに注力する。