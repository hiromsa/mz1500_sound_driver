# FM音源GUIエディタ 実装およびUIデザイン要求仕様

## 1. 概要
MZ-1500 Sound Systemの統合環境（IDE）内で動作する、FM音源（YM2151）用のGUI音色エディタ。MMLのテキストベースでの高速な入力を阻害せず、GUIでの視覚的なパラメータ調整と双方向でデータを連動させる。初心者から熟練者まで、目的に応じて最適なアプローチで音作りを行えるモダンな開発環境を提供する。

## 2. UI/UXデザイン要件（VS Codeスタイル）
レトロPC界隈のエンジニア層が好む、ストイックで効率的な「VS Code（統合開発環境）風」のモダンデザインを採用する。以下のガイドラインに従って、Avalonia UI（XAML）による画面構築を行うこと。

*   **ベーステーマ:** プロフェッショナル向けのダークテーマ。無駄なグラデーションやドロップシャドウを完全に排した「フラット＆ミニマル」なデザインとする。
*   **カラーパレットの統一:** 
    *   背景色: 深みのあるソリッドなダークグレー（例: `#1E1E1E` または `#252526`）。
    *   パネル背景/区切り: 要素の区切りは枠線（Border）ではなく、背景色のわずかな明度差（例: `#2D2D30`）で表現する。
    *   文字色: 可読性の高い明るいグレー（例: `#CCCCCC`）。
    *   アクセントカラー: アクティブなスライダーやフォーカスされている要素には、ブルー（`#007ACC`）やシアンを使用し、視線を誘導する。
*   **境界線の強調 (ソリッドボーダー):** 各オペレータパネル等の区切りに極細（1px）の暗いボーダー（例: `#404040`）を付与する。
*   **フォントとテキストのコントラスト:** パラメータのラベルは小さく暗いグレー（例: `10pt`, `#888888`）とし、入力される「数値」は明るい白（例: `#E0E0E0`）の太字（Bold）とする。
*   **入力コントロールの立体感:** スピンボックス等の背景色はパネルより一段暗い色（例: `#181818`）にする。
*   **コントロールの高さ・余白の極限最適化 (重要):** テキストボックスやスピンボックスなどのコントロールが、入力値に対して不必要に高くなっている。`MinHeight` の無効化や、`Padding="2,0"` 等の指定により縦のサイズを極限まで低くし、もっさり感を完全に排除すること。Marginも「2, 4, 8」の倍数で厳密に統一し、高密度グリッドを形成する。
*   **ツールチップとヒントマーク:** 各パラメータのラベル付近に、小さく目立たないヒントマーク（例: `[?]` アイコンや情報アイコン）を配置し、マウスオーバー時にそのパラメータの意味を解説するツールチップを表示させること。
*   **UI機能性と視認性の担保:** ウィンドウのリサイズや要素の再配置が行われた際に、入力項目が見切れたり操作不能にならないよう、`ScrollViewer` の適切な配置やウィンドウの最小サイズを設定し、UIとしての機能性を完全に担保すること。
*   **角丸 (Rounded Corners):** ボタンや入力フィールドの角は完全にゼロ（`CornerRadius="0"`）か極めてわずかな丸み（`CornerRadius="2"`）にとどめる。

## 3. 起動とデータ連携（入出力・バインディング）
*   **起動トリガー:** MMLエディタ上で `@FM[番号] = { ... }` 内で右クリックし「FM音色エディタを開く」を選択して起動。
*   **初期データ読み込み:** 起動時にMMLテキストをパースし、ViewModelの46個のプロパティに流し込む。
*   **MMLへの反映 (双方向バインディング):** 「反映 (Apply)」ボタン押下時、現在のViewModelをテキストフォーマットにシリアライズし、MMLエディタ側の文字列を置換・更新してウィンドウを閉じる。

## 4. 画面レイアウトとUIコンポーネント構成
UIはMVVMパターンに基づき、以下の3つのセクションで構成する。

### 4.1 ヘッダー部（グローバル設定）
*   **アルゴリズム (ALG):** 0〜7の選択。接続構造がわかるブロック図アイコン付きのトグルボタンを配置。
*   **フィードバック (FB):** 0〜7の数値指定。スライダーと数値入力コントロールをバインド。

### 4.2 オペレータ編集部（OP1〜OP4）
4つのオペレータのパラメータ群を2行2列、または横4列のグリッドで配置する。
*   **視覚的補助:** ALGに応じて「キャリア」「モジュレータ」を判定し、パネルの背景色をわずかに変化させる。

各オペレータパネル内の構成要素：
*   **ヘッダー:** 「OP1 (M1)」等のラベルと、右端に [Mute] / [Solo] トグルボタン、[コピー＆ペースト] アイコンを小さくフラットに配置。
*   **トータルレベル (TL):** 0〜127（無音）。縦型スライダーで配置し、値の領域をアクセントカラーで塗りつぶすメーター風にする。
*   **ピッチ系パラメータ:** `MUL` (0〜15)、`DT1` (0〜7)、`DT2` (0〜3)。横幅を `Width="50"` 程度に固定し、高さを抑えたスピンボックスでGridに整列させる。ラベルの横にヒントマークを配置。
*   **エンベロープ表示＆編集部（最重要UI）:**
    *   **数値入力部:** AR, D1R, D2R, RR (0〜31)、D1L (0〜15) の数値を入力するスピンボックスを横並びに配置（ヒントマーク付き）。
    *   **グラフの完全表示 (見切れ防止):** これら数値から計算した折れ線グラフをCanvasで視覚化する。グラフの頂点（最大音量時など）がCanvas領域の外にはみ出して見切れ（クリッピング）ないよう、Canvas内のY軸スケーリング処理を正しく行うか、適切なPaddingを設けること。高さは `Height="100"`〜`120` 程度に制限する。
    *   **フル・インタラクティブ・ハンドル:** グラフ上に、**AR, D1R, D2R, RR, D1L の5つすべてのポイントに対する操作ハンドル（頂点ノード）を必ず配置すること**。マウスドラッグにより全パラメータを直接増減でき、グラフ全体が動的に再描画されるようにする。

### 4.3 フッター部（プレビューと操作）
*   **プレビュー鍵盤:** マウスでクリック可能なフラットデザインの簡易ピアノロールをバランスよく配置。
*   **リアルタイム発音機能:** 鍵盤やPCキーボードの押下イベントをフックし、内部プレイヤ（NAudio）へパラメータを送信してリアルタイム発音させる。
*   **操作ボタン:** 右下に「反映 (Apply)」「キャンセル (Cancel)」を配置。反映ボタンのみアクセントカラーで塗りつぶす。

## 5. 内部ロジックおよび実装時の注意事項
*   **UIとロジックの完全分離 (MVVM):** 46個のプロパティと通知イベントを持つViewModelを構築し、XAML（見た目）とデータ処理（裏側）を完全に分離すること。
*   **波形プレビュー（オシロスコープ）の除外:** 合成波形のCanvas描画機能はスコープ外。エンベロープ形状のグラフ化と高速なレスポンスに注力する。