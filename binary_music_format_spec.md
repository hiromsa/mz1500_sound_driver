# MZ-1500 Sound System: Z80 Binary Music Format Specification

## 概要
本ドキュメントは、MMLコンパイラが出力し、Z80サウンドドライバが解釈するための「中間バイナリデータ形式」の仕様書です。
MZ-1500のメモリ制約（64KB）の中でPCGやOSと共存できるよう、将来のFM音源（YM2151）対応を見据えた課題と、フォーマットの刷新案をまとめています。

---

## 1. 現在のフォーマット仕様と課題

現在のフォーマットは、処理の簡略化を優先して設計されているため、データサイズが非常に大きい（冗長である）という課題があります。

### 1-1. 現在のコマンド一覧

| コマンド名 | Opcode | 引数 | 合計サイズ | 処理内容 |
| :--- | :--- | :--- | :--- | :--- |
| `CMD_TONE` | `0x01` | レジスタ(2バイト) + 長さ(2バイト) | 5 bytes | PSG/BEEPのトーン発音 |
| `CMD_REST` | `0x02` | 長さ(2バイト) | 3 bytes | PSG/BEEPの休符 |
| `CMD_VOL` | `0x03` | 音量(1バイト) | 2 bytes | PSGのハードウェア音量設定 |
| `CMD_ENV` | `0x04` | エンベロープID(1バイト) | 2 bytes | ソフトウェア音量エンベロープ設定 |
| `CMD_PENV` | `0x05` | ピッチEnv ID(1バイト) | 2 bytes | ピッチエンベロープ設定 |
| `CMD_NOISE` | `0x06` | ノイズ設定(1バイト) + 長さ(2バイト) | 4 bytes | ノイズ発音 |
| `CMD_WAIT` | `0x20` | フレーム数(2バイト) | 3 bytes | FM音源用のウェイト（待機） |
| `CMD_YM2151_REG`| `0x21` | レジスタ番号(1バイト) + 値(1バイト) | 3 bytes | FM音源への直接レジスタ書き込み |
| `CMD_END` | `0xFF` | なし | 1 byte | 曲の終わり |

### 1-2. データ構造の可視化（現状）

1つの音符（例：FM音源で「ド」を鳴らす）を出力するだけでも、大量のバイト数を消費します。

```mermaid
block-beta
  columns 1
  block:CurrentFM["現在のFM音源データ出力例（1音鳴らすだけでも巨大）"]
    A["音色設定 (26レジスタ × 3バイト = 78バイト)"]
    B["音階設定 (2レジスタ × 3バイト = 6バイト)"]
    C["Key ON (1レジスタ × 3バイト = 3バイト)"]
    D["Wait (3バイト)"]
    E["Key OFF (1レジスタ × 3バイト = 3バイト)"]
  end
```

### 1-3. 現状の主な問題点

> [!WARNING]
> **メモリ枯渇の危険性**
> MZ-1500のメインメモリは64KBしかなく、現在の仕様のまま和音や長時間のFM音源再生を行うと、すぐにメモリを使い果たしてしまいます。

1. **Z80再生ルーチンのコード重複:** 現在のコンパイラ（`MZ1500MusicAssembler.cs`）は、**全チャンネル分（PSGやBEEP等）全く同じ再生ルーチンを展開・複製**しています。チャンネルが増えるほどZ80のプログラムサイズが非効率に肥大化します。
2. **FM音源のレジスタベタ打ち:** 音色の切り替えやキーオンオフを直接レジスタ書き込み（3バイト）で行っているため、データが爆発的に増えます。
3. **音符長が常に2バイト:** 同じ長さの音符（8分音符の連続など）でも、毎回「2バイトの長さデータ」を付与しており無駄が多いです。
4. **Opcodeの無駄:** コマンドの先頭に必ず1バイトの識別子（Opcode）が必要で、1音鳴らすためのオーバーヘッドが大きいです。

---

## 2. 改善案：新バイナリフォーマット（1バイトコマンドベース）

データサイズとドライバのコードサイズを劇的に削減するため、以下の設計に変更します。

### 2-1. Z80ドライバの共通化（ワークエリア方式）

チャンネルごとに再生ルーチンを複製するのをやめ、**共通の再生サブルーチン（1つだけ）**を作成します。
各チャンネルの状態（現在のポインタ、長さ、エンベロープ状態など）は「チャンネル・ワークエリア（RAM）」に配置し、Z80の `IX` または `IY` レジスタを使ってワークエリアを参照しながら共通ルーチンを呼び出す設計にします。これによりドライバ側のコードサイズが激減します。

### 2-2. データ構造の可視化（改善案）

```mermaid
block-beta
  columns 1
  block:Proposed["改善案のデータ出力例（大幅な削減）"]
    A["音色指定コマンド (2バイト) ※テーブル参照"]
    B["長さ指定コマンド (2バイト) ※連続する場合は省略"]
    C["音階コマンド (1バイト) ※キーオン/オフ自動化"]
  end
```

### 2-3. 新フォーマットの設計方針

1. **FM音源の抽象化（Voice ID化）**
   - 音色データ（約25バイト）はZ80側のメモリ上に「テーブル」として事前に配置します。
   - 曲データ中には「Voice ID（1バイト）」のみを記述し、ドライバがテーブルからYM2151へ一括転送します。
2. **デフォルト音符長（Step）の導入**
   - 「現在の音符の長さ」を保持するコマンドを追加し、同じ長さの音符が続く場合は長さデータの出力を省略します。
3. **バイトコードパッキング**
   - データの**最上位ビット(MSB)や上位ビット**を利用して、「コマンド」か「音符」かを1バイトで判別できるようにします。

### 2-4. 新コマンドマップ案（1バイトで判別）

以下の表のように、先頭1バイトの値を見るだけで、そのデータが「音階」なのか「コマンド」なのかが分かるようにします。

| バイト値 (Hex) | 種類 | 引数サイズ | 内容・処理 |
| :--- | :--- | :--- | :--- |
| `0x00` ～ `0x5F` | **Note ON (音符)** | 0 byte | `0～95` の音階番号。直前に設定された「音符長」「音色」を使って発音する。 |
| `0x60` | **Rest (休符)** | 0 byte | 音を止め、現在の音符長だけ待機する。 |
| `0x80` ～ `0x8F` | **長さ変更 (短)** | 0 byte | よく使う短いフレーム数（1〜16フレームなど）への長さ変更。 |
| `0x90` | **長さ変更 (長)** | 1～2 bytes | 任意のフレーム数へ長さを変更する。 |
| `0xA0` | **音色変更 (Voice)**| 1 byte | FM音源の音色、またはPSGのエンベロープIDを変更する。 |
| `0xA1` | **音量変更 (Vol)** | 1 byte | ボリュームを変更する。 |
| `0xFF` | **Track End** | 0 byte | 曲の終了。 |

> [!TIP]
> **削減効果**
> この新フォーマットであれば、同じ長さの音符が連続する場合、**1音符あたりわずか「1バイト」**で表現できるようになります。現在の5バイト～数十バイトと比較して、劇的なメモリ削減が見込めます。

---

## 3. 次のステップ

Z80アセンブラの実装に入る前に、以下の手順で進めることを推奨します。

1. **コンパイラの改修:** `MmlToZ80Compiler.cs` を改修し、上記の「新コマンドマップ」に従ったバイナリを出力するように変更する。
2. **ダンプツールの作成:** 出力されたバイナリが意図通りにパッキングされているか確認できるツールを作成する。
3. **Z80ドライバの実装:** 新フォーマットを解釈し、ワークエリア方式で動作する共通ルーチンとしてZ80アセンブラを実装する。

> [!CAUTION]
> **デグレード（機能後退）の防止と慎重な作業**
> フォーマットとドライバの全面的な刷新となるため、バグが混入すると「コンパイラ側が悪いのか」「Z80ドライバ側が悪いのか」の切り分け・調査に膨大な時間がかかります。
> デグレードを絶対に防ぐため、変更は一度に全て行わず、**「ダンプツールによる出力バイナリの目視確認」**や**「単一チャンネル（BEEPのみ等）でのミニマムな動作テスト」**を挟みながら、確実に1歩ずつ慎重に作業を進めてください。
